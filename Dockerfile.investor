# Dockerfile for Investor Management Service

FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

CMD ["uvicorn", "ultracore.api.investor_api:router", "--host", "0.0.0.0", "--port", "8000"]
